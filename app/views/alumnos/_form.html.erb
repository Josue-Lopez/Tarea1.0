<%= form_with(model: alumno, local: true) do |form| %>
  <% if alumno.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alumno.errors.count, "error") %> prohibited this alumno from being saved:</h2>

      <ul>
      <% alumno.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre, id: :alumno_nombre %>
  </div>

  <div class="field">
    <%= form.label :edad %>
    <%= form.number_field :edad, id: :alumno_edad %>
  </div>

  <div class="field">
    <%= form.label :identidad %>
    <%= form.text_field :identidad, id: :alumno_identidad %>
  </div>

  <div class="field">
    <%= form.label :cuenta %>
    <%= form.number_field :cuenta, id: :alumno_cuenta %>
  </div>

  <div class="field">
    <%= form.label :direccion %>
    <%= form.text_field :direccion, id: :alumno_direccion %>
  </div>

  <div class="field">
    <%= form.label :carrera %>
    <%= form.text_field :carrera, id: :alumno_carrera %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
